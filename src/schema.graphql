# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

input CreateUserArg {
  age: Int!
  email: String!
  name: String!
}

type Mutation {
  createUser(userData: CreateUserArg!): User!
  dumpUsers(amount: Int!): [User!]!
}

type Query {
  hello(name: String!): String!
  helloWorld: String!
  users(options: optionArgs!): UserPagination!
}

type User {
  age: Int
  email: String
  name: String
}

type UserPagination {
  docs: User!
  hasNextPage: Boolean!
  hasPrevPage: Boolean!
  limit: Int!
  nextPage: Int!
  page: Int!
  pagingCounter: Int!
  prevPage: Int!
  totalDocs: Int!
  totalPages: Int!
}

input optionArgs {
  lean: Boolean
  leanWithId: Boolean
  limit: Int
  offset: Int
  page: Int
  pagination: Boolean
}
